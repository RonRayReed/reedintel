# Copy this file to .env — never commit .env to git

# Local dev (matches docker-compose defaults)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=reedintel
DATABASE_USER=reedadmin
DATABASE_PASSWORD=reedlocal2024
DATABASE_SSLMODE=disable

# Azure PostgreSQL (production) — set these as Container App env vars instead
# DATABASE_HOST=reedintel-pg-prod.postgres.database.azure.com
# DATABASE_PASSWORD=your_strong_password
# DATABASE_SSLMODE=require
